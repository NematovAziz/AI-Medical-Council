services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: medical_council
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: [medical_council_pg:/var/lib/postgresql/data]
volumes:
  medical_council_pg:
